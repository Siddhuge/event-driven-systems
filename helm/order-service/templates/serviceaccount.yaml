{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "event-driven-azure.serviceAccountName" . }}
  labels:
    {{- include "event-driven-azure.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end -}}
